import React from 'react';
import logo from '../assets/logo.png';

function Logo() {
    return(
        <div className="App-logo">
            <img src={logo} alt=''/>
        </div>
    );
}

export default Logo;